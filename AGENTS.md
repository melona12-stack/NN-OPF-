# AGENTS.md — 새로 합류한 에이전트에게

이 저장소를 처음 여는 코딩 에이전트(Codex, Claude Code, 그 밖에 무엇이든)가
**가장 먼저 읽을 파일**입니다. 사람용 소개는 [`README.md`](README.md) 에,
연구 내용은 `docs/` 에 있습니다. 여기에는 **일하는 방식과 지금 상태**만
적습니다.

---

## 1. 이 프로젝트가 무엇인가 — 세 문장

전력계통의 **최적조류계산(AC-OPF)** 에서 가장 비싼 부분인 조류방정식 풀이를
**신경망 대체모델**로 갈아끼우는 12개월 연구입니다. 지금은 **4단계(M4)** —
조류계산 대체모델을 학습시켜 **선형 근사 기준선을 이기는 것**이 목표입니다.

주장할 것은 두 가지입니다: **(A)** 세 가지 삽입 방식을 같은 계통·데이터·지표로
처음 비교한 **통합 벤치마크**, **(B)** 그래프·물리정보·볼록성을 동시에 만족하는
**IC-GNN** 대체모델. ([`docs/00_overview.md`](docs/00_overview.md) §7)

---

## 2. 일하는 방식 — 이 팀의 규칙

> [!IMPORTANT] 역할 분담이 고정되어 있습니다
> - **에이전트가 코드를 전부 씁니다.** 사람(조성현)은 코드를 직접 고치지
>   않습니다.
> - **사람이 GPU 작업을 돌립니다.** VS Code 작업 목록(`.vscode/tasks.json`)에서
>   골라 실행하고 결과를 보고합니다. 에이전트는 이 컨테이너에 GPU 가 없습니다.
> - **에이전트가 문서와 Notion 을 관리합니다.**
>
> 그래서 에이전트가 할 일은 이렇게 순환합니다:
> **① 결과를 읽고 → ② 진단하고 → ③ 다음 실험을 `tasks.json` 에 세우고 →
> ④ 문서에 적고 → ⑤ 사람에게 "이걸 돌리세요" 라고 한 줄로 말한다.**

### 사람에게 말할 때

- 한국어로 씁니다.
- **결론을 먼저**, 그다음 숫자, 그다음 근거. 서론 금지.
- 실험 지시는 **"작업 N번을 돌리세요"** 한 줄로 끝냅니다. 그 사람은 목록에서
  번호만 찾습니다.
- **내가 틀렸으면 틀렸다고 먼저 말합니다.** 이 프로젝트는 실패한 처방을
  문서에 남기는 것이 규칙입니다 (§5 참조).

### 브랜치와 커밋

- 개발 브랜치: `claude/nn-opf-model-development-yfny1v`
  (새 에이전트는 자기 이름의 브랜치를 새로 파도 됩니다. 다만 **하나만** 쓰세요.)
- 커밋 메시지는 **한국어**, 본문에 "무엇을 왜 바꿨고 그래서 숫자가 어떻게
  됐는지" 를 적습니다. 이 저장소의 커밋 로그가 곧 연구 일지입니다.
- **모델 식별자(모델 이름·버전)를 커밋 메시지, PR, 코드 주석, 저장소에
  들어가는 어떤 산출물에도 넣지 않습니다.**
- PR 은 **명시적으로 요청받았을 때만** 만듭니다.
- 푸시: `git push -u origin <branch>`. 네트워크 실패 시 2s→4s→8s→16s 로 최대
  4회 재시도.

---

## 3. 저장소 지도

```
src/nnopf/          핵심 라이브러리 (§8 대조표 참조)
scripts/s00~s10     실행 스크립트. 번호가 곧 파이프라인 순서
tests/              94개. 모든 함정이 여기 고정돼 있다 — 반드시 통과시킬 것
docs/00~08          연구 문서. 번호 순서대로 읽으면 된다
tools/              Notion 동기화 (§6)
.vscode/tasks.json  실험 목록 61개 — 사람이 여기서 골라 돌린다
results/            실행 결과 JSON. 사람이 GPU 로 돌리고 커밋해 올린다
data/               데이터셋 (.npz 는 gitignore — 시드 고정이라 재생성 가능)
```

### 문서를 어디부터 읽을까

| 급할 때 | 읽을 것 |
|---|---|
| 5분 안에 상황 파악 | `docs/00_overview.md` §4 (지금 어디까지 왔나) + 이 파일 §4 |
| 어떤 수식이 어느 코드인지 | **`docs/08_math_reference.md`** — 식 64개와 그 코드 |
| 지금까지의 실험 전부 | `docs/06_surrogate_training.md` (제일 큼, §6.1 이 본론) |
| 이미 밟은 함정 | `docs/06` §7 (열 개), 그리고 이 파일 §5 |
| 왜 이 연구를 하는가 | `docs/00_overview.md` §7.1 (SLSQP 가 이미 푸는데 왜?) |

---

## 4. 지금 상태 — 2026-08 기준

### M4 벤치마크: 네 칸 중 세 칸 승

| 시험 | 선형 최소제곱 | 신경망 최고 | |
|---|---|---|---|
| case30 무작위 (21,000) | 1.000% | MLP **0.347%** | 신경망 2.9배 승 |
| case30 미지 N-1 (30,000) | 2.047% | MLP+`lstsq` **1.455%** | 신경망 1.41배 승 |
| case118 무작위 (60,000) | 0.608% | MLP+`lstsq`+λ **0.592%** | 신경망 1.03배 승 |
| case118 미지 N-1 (60,000) | **1.010%** | MLP+`lstsq`+λ 1.020% | **선형 1.0% 승** |

지표는 **P/부하 %** — 표본당 $\sum_i|\Delta P_i|$ 를 총부하로 나눈 값입니다
(`docs/08` §9).

### 그런데 M4 는 끝이 아닙니다 — 열려 있는 것 셋

**① 그래프 신경망이 MLP 를 한 번도 못 이겼습니다.**
case30 미지 N-1 에서 GAT 최고 2.688% vs MLP+`lstsq` 1.455% — **1.85배 집니다.**
M4 표를 채우는 건 전부 MLP 이고, 논문의 본론(GAP 1, IC-GNN)은 그래프 쪽입니다.
**여기가 진짜 남은 일입니다.**

**② case30 두 칸을 야코비안 선형화에 집니다** (`docs/06` §7.9).
데이터를 한 건도 안 보는 물리 선형화가 case30 에서 **0.145%** 입니다 —
우리 최고에 2.4배·10배 앞섭니다. case118 에서는 뒤집히지만(1.953% vs 0.592%),
**"작은 계통에서는 학습이 필요 없다"** 를 우리가 먼저 정직하게 써야 합니다.

**③ 마지막 칸(case118 미지 N-1)의 남은 축은 토폴로지 다양성뿐입니다.**
λ 도(작업 48·50) 표본 수도(작업 52) 소진했습니다. case118 은 고정된 177개
상정사고뿐이라 표본을 100k 로 늘리면 오히려 나빠집니다. 남은 것은 **계통 혼합
또는 N-2** 입니다.

### 바로 다음에 할 일

`.vscode/tasks.json` 에서 **`▶` 표시가 붙은 것**이 지금 할 것입니다.
현재는 **작업 60·61** — 게이팅을 끈 GAT 에 λ=5e-3 과 λ=0 을 걸어, "GAT 은
이미 구조로 그래프를 알아서 물리손실이 새 정보를 안 준다" 는 짐작을 확인하는
짝입니다.

---

## 5. 이미 밟은 함정 — 다시 밟지 마세요

`docs/06` §7 에 열 개가 자세히 있고, 아래는 **새 에이전트가 특히 재발시키기
쉬운 것**만 추렸습니다. 전부 테스트로 고정돼 있으니 `pytest` 가 잡아 줍니다.

| 함정 | 무슨 일이 났나 | 어디 |
|---|---|---|
| 슬랙 기준위상을 0 으로 가정 | case118 슬랙은 30°. 결과가 통째로 오염 | §7.4 |
| 손실을 원단위로 계산 | MSE 가 1e-4 라 weight decay 가 학습을 죽임 | §7.2 |
| 선로상태 열을 표준화 | 미지 N-1 에서 정규화 입력이 1e6 으로 폭주 | §7.1 |
| 최소제곱에서 상수열을 안 뺌 | 조건수 2.7e58, **컴퓨터마다 답이 다름** | §7.6 |
| 잔차를 float32 로 측정 | 측정 바닥이 재려는 값과 같은 자릿수 | §7.3 |
| 전압 위반 지표에 허용오차 없음 | case118 이 항상 정확히 0.8475% (인공물) | §7.10 |
| GAT 층 수를 계통 지름보다 작게 | 정보가 반대편 모선까지 못 감 | §7.7 |
| 미지 N-1 에서 검증손실로 모델 선택 | 거의 학습 안 된 모델이 뽑힘 | §7.8 |
| 두 실험을 **동시에** 돌리고 시간 비교 | 물리손실 비용을 55% 로 잘못 읽음 (실제 +7.4%) | §6.1 |

> [!CAUTION] 이 프로젝트의 가장 비싼 교훈 하나
> **`crit` 신호 하나가 모델 선택 · 조기 종료 · 학습률 스케줄러 셋을 다 몹니다.**
> 셋이 원하는 성질이 다릅니다 (보고할 값 / 추세를 읽을 것 / 조용할 것).
> 이 하나가 **세 번** 사고를 냈습니다 — 모델 선택(§7.8), 학습률 붕괴(작업 51),
> 조기 종료(작업 57). 미지 N-1 에서는 `val_phys` 가 epoch 마다 3.07% 튀고
> `val` 은 바닥에 닿아 평평해집니다. **어느 쪽을 물려도 오작동합니다.**
>
> 새 실험을 설계할 때 이 셋을 분리해 두었는지 먼저 확인하세요
> (`--select`, `--patience`, `--sched-on`, `--lr-patience`).

### 실패한 처방도 문서에 남깁니다

이 저장소의 규칙입니다. 예를 들어 §6.2(야코비안 가중 손실)는 **효과가 없었다**
는 결론이 그대로 남아 있고, §6.1 에는 제가 처방했다가 틀린 `--sched-on loss`
이야기가 정정과 함께 남아 있습니다. **지우지 마세요.** 같은 곳을 다시 파는
것을 막는 것이 이 문서들의 절반입니다.

---

## 6. Notion 동기화

문서는 GitHub 과 Notion 양쪽에 있습니다. 페이지 주소는
[`docs/notion_links.json`](docs/notion_links.json) 에 있습니다.

```bash
python tools/md_to_notion.py docs/06_surrogate_training.md
# -> docs/06_surrogate_training.notion.md 를 만든다 (gitignore 대상)
```

`tools/md_to_notion.py` 는 GitHub 마크다운을 Notion 문법으로 바꿉니다
(파이프 표 → `<table>` XML, `$x$` → `` $`x`$ ``, `> [!NOTE]` → `<callout>`).

> [!IMPORTANT] Notion 의 `update_content` 는 **저장된 형태와 정확히 같아야** 매칭됩니다
> Notion 은 마크다운을 블록으로 파싱한 뒤 다시 직렬화하면서 정규화합니다 —
> 표 안쪽 들여쓰기를 없애고, 짝이 안 맞는 `**` 를 버리고, `| ~ [ ]` 를
> 이스케이프합니다. `tools/notionize.py` 가 그 규칙을 재현합니다.
>
> ```bash
> python tools/notionize.py docs/06_surrogate_training.notion.md
> ```
>
> **실전 요령**: 긴 블록을 한 번에 매칭시키려 하지 마세요. **제목 한 줄**을
> `old_str` 로 잡고 그 앞에 새 내용을 끼워 넣는 것이 가장 안전합니다.
> 표 안쪽 줄은 들여쓰기가 **없는** 형태로 매칭해야 합니다.

---

## 7. 명령어 모음

```bash
# 환경
pip install -r requirements.txt
python scripts/s00_check_env.py          # GPU 실연산까지 확인

# 데이터 (시드 고정 — 어디서 돌려도 같은 데이터)
python scripts/s02_generate_dataset.py --case case30 -n 30000
python scripts/s06_data_fingerprint.py   # 내 데이터가 남과 같은지 지문 확인

# 검증
pytest -q                                # 94개. 전부 통과해야 한다

# 학습 (사람이 GPU 에서 돌리는 것)
python scripts/s03_train_surrogate.py --case case30 -n 30000 --epochs 1500 \
    --lr 5e-4 --model gat --layers 8 --split unseen-n1 --select phys \
    --lam 5e-3 --tag _myrun

# 측정
python scripts/s09_physical_baselines.py   # DC·야코비안 선형화 비교군
python scripts/s10_inference_benchmark.py  # 추론 속도 (CPU/GPU × 배치)
python scripts/s05_device_benchmark.py     # 학습 속도
```

### 자주 쓰는 학습 옵션 — 뜻과 함정

| 옵션 | 뜻 | 주의 |
|---|---|---|
| `--split unseen-n1` | 시험용 상정사고를 학습·검증에서 **완전히** 뺌 | 이게 P2 의 핵심 실험 |
| `--select phys` | 검증 **P/부하 %** 로 모델 선택 | 미지 N-1 에서는 필수 (§7.8) |
| `--skip-init lstsq` | 선형 지름길을 최소제곱 해에서 출발 | **lr 을 같이 낮추면 안 됨** — epoch 1~2 에서 멈춤 |
| `--lam 5e-3` | 물리손실. **지도 항 대비 비율** | MLP 엔 듣고 GAT 엔 안 듦 (작업 59) |
| `--lr-patience 2000` | 학습률 스케줄러를 사실상 끔 | epoch 수보다 크게 |
| `--patience 2000` | 조기 종료를 끔 | 위와 짝 |
| `--vm-head raw` | 시그모이드 전압 헤드 절제 | 기본 `scaled` 가 P/부하에 9.4% 유리 |
| `--no-gate` | 끊긴 선로의 어텐션 차단을 끔 | GAT 의 존재 이유를 재는 절제 |
| `--resume` | 체크포인트에서 이어 돌림 | 난수 상태까지 복원 — 비트 단위 동일 |

---

## 8. 코드 ↔ 수식 대조는 별도 문서에 있습니다

새 에이전트가 코드를 이해할 때 가장 빠른 길입니다.

**→ [`docs/08_math_reference.md`](docs/08_math_reference.md)**

식 64개와 그것을 계산하는 코드 39조각을 파일·함수 이름과 함께 붙여 놓았습니다.
$Y_{bus}$ 조립부터 뉴턴 야코비안, AC-OPF 제약, 물리정보 손실, 어텐션,
평가 지표까지. 마지막 §12 는 **"식만 적으면 재현되지 않는 것"** 여섯 가지입니다.

| 파일 | 담고 있는 식 |
|---|---|
| `ybus.py` | π 등가회로, $Y_{bus}$ 조립, 브랜치 조류 |
| `powerflow.py` | $S = V\overline{YV}$, $\partial S/\partial V$, 뉴턴 반복 |
| `opf.py` | AC-OPF 정식화, 제약과 그 야코비안 |
| `dataset.py` | 시나리오 샘플링, 급전 배분, NumPy 판 물리 잔차 |
| `physics_torch.py` | 미분 가능한 물리 잔차, N-1 4성분 보정, 물리 손실 |
| `models.py` | MLP 입출력 계약, 전압 헤드, 선형 지름길 |
| `gnn.py` | 엣지 어텐션, 게이팅, 모선별 헤드 |
| `train.py` | 지도 손실, λ 스케줄, lstsq 초기화, 평가 지표 |
| `baselines.py` | 최소제곱 선형, DC 조류계산, 야코비안 선형화 |

---

## 9. 인수인계 체크리스트

새 에이전트가 첫 턴에 할 일:

- [ ] 이 파일을 읽는다 (지금 하고 있음)
- [ ] `docs/00_overview.md` §4 와 §7 을 읽는다 — 목표와 주장
- [ ] `docs/08_math_reference.md` 를 훑는다 — 코드가 뭘 계산하는지
- [ ] `.vscode/tasks.json` 에서 `▶` 항목을 확인한다 — 지금 할 실험
- [ ] `docs/06_surrogate_training.md` §10 체크리스트를 본다 — 열린 항목 전부
- [ ] `pytest -q` 로 94개가 통과하는지 확인한다
- [ ] `git log --oneline -30` 으로 최근 흐름을 본다

그리고 사람에게 **"작업 N번을 돌리세요"** 한 줄로 시작하면 됩니다.
